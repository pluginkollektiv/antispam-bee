<?xml version="1.0"?>
<ruleset name="WordPress Coding Standards based custom ruleset for your plugin">
	<description>Generally-applicable sniffs for WordPress plugins.</description>
	<!--
	#############################################################################
	COMMAND LINE ARGUMENTS
	https://github.com/squizlabs/PHP_CodeSniffer/wiki/Annotated-ruleset.xml
	#############################################################################
	-->

	<!-- Pass some flags to PHPCS:
		 p flag: Show progress of the run.
		 s flag: Show sniff codes in all reports.
	-->
	<arg value="ps"/>

	<!-- Strip the filepaths down to the relevant bit. -->
	<arg name="basepath" value="./"/>

	<!-- Check up to 8 files simultaneously. -->
	<arg name="parallel" value="8"/>

	<!-- Only check the PHP. CSS/SCSS/JS files are checked separately with linters. -->
	<arg name="extensions" value="php"/>

	<!-- Enable colors in the output -->
	<arg name="colors"/>

	<!-- The paths that hold the plugin's own PHP, listed explicitly the same way
		 `phpstan.neon` declares its `paths`. An allowlist keeps the run from walking
		 the whole working tree, which matters because a checkout can contain build
		 output, caches, and the working directories of AI agents — `.claude/worktrees/`
		 alone held tens of thousands of PHP files here. -->
	<file>antispam_bee.php</file>
	<file>phpstan-bootstrap.php</file>
	<file>rector.php</file>
	<file>src</file>

	<!--
	#############################################################################
	EXCLUDE SOME FILES AND FOLDERS
	#############################################################################
	-->

	<exclude-pattern>/build/</exclude-pattern>
	<exclude-pattern>/vendor/</exclude-pattern>
	<exclude-pattern>/node_modules/</exclude-pattern>
	<exclude-pattern>/tests/</exclude-pattern>

	<!-- The `<file>` list above already keeps the default run out of these, but a path
		 passed on the command line (by an IDE, or `phpcs .`) overrides that list, so the
		 patterns stay as a safety net.

		 Exclude patterns are regular expressions, so this one covers every hidden
		 directory at once: editor state (`.idea/`) and the working directories AI
		 agents create (`.claude/`, `.aider/`, `.cursor/`, `.windsurf/`, …). Those can
		 hold entire checkouts of this repository, and a contributor may well have dot
		 directories no list here could predict. -->
	<exclude-pattern>/\.[^/]+/</exclude-pattern>

	<!-- Local scratch and tool output. Both are in `.gitignore`. -->
	<exclude-pattern>/tmp/</exclude-pattern>
	<exclude-pattern>/test-results/</exclude-pattern>

	<!--
	#############################################################################
	USE THE WordPress RULESET
	#############################################################################
	-->

	<!-- https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards -->
	<!-- https://github.com/WordPress-Coding-Standards/WordPress-Coding-Standards/wiki/Customizable-sniff-properties -->
	<!-- Keep in sync with "Requires at least" in readme.txt. -->
	<config name="minimum_supported_wp_version" value="4.7"/>

	<rule ref="WordPress">
		<!-- As we use PHP 5.4+ now, we can use the short array syntax -->
		<exclude name="Universal.Arrays.DisallowShortArraySyntax.Found"/>
		<!-- Uncomment if you develop a theme with templates for CPTs having a prefix -->
		<exclude name="WordPress.Files.FileName"/>
		<!-- Uncomment for projects using the metabox toolbox -->
		<!--<exclude name="WordPress.CSRF.NonceVerification.NoNonceVerification" />-->
		<!-- Uncomment if you want to use some functions that should be replaced with the VIP cached alternatives -->
		<!--<exclude name="WordPress.VIP.RestrictedFunctions" />-->
		<!--<exclude name="WordPress.VIP.PostsPerPage" />-->
		<exclude name="Squiz.Commenting.FunctionComment.EmptyThrows"/>
		<exclude name="Universal.Operators.DisallowShortTernary.Found"/>
	</rule>
	<rule ref="WordPress.NamingConventions.PrefixAllGlobals">
		<properties>
			<!-- Value: replace the function, class, and variable prefixes used. Separate multiple prefixes with a comma. -->
			<property name="prefixes" type="array">
				<element value="antispam_bee"/>
				<element value="AntispamBee"/>
			</property>
		</properties>
	</rule>
	<rule ref="WordPress.WP.I18n">
		<properties>
			<!-- Value: replace the text domain used. Separate multiple prefixes with a comma. -->
			<property name="text_domain" type="array">
				<element value="antispam-bee"/>
			</property>
		</properties>
	</rule>
	<rule ref="WordPress.WhiteSpace.ControlStructureSpacing">
		<properties>
			<property name="blank_line_check" value="true"/>
		</properties>
	</rule>


	<!--
	#############################################################################
	USE THE PHPCompatibility RULESET
	#############################################################################
	-->

	<!-- https://github.com/PHPCompatibility/PHPCompatibility#sniffing-your-code-for-compatibility-with-specific-php-versions -->
	<config name="testVersion" value="7.4-"/>
	<!-- https://github.com/PHPCompatibility/PHPCompatibilityWP -->
	<rule ref="PHPCompatibilityWP"/>

</ruleset>
